Open a terminal and write docker-compose up -d. You need Docker Desktop installed. This is windows 10 version 
for running this project on localhost. 

It requires .env file. Check dummy values in .env file.